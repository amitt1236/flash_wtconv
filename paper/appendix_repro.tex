Every number in this paper is produced by a script in the accompanying
artifact, and no value is transcribed into the manuscript by hand: the
benchmark scripts emit JSON, a table generator turns that JSON into the
\LaTeX{} fragments the paper \verb|\input|s, and re-running the pipeline
overwrites the tables in place.

\subsection{Pipeline}

\begin{verbatim}
python scripts/bench.py        --device cuda --protocol both --mode both \
                               --out results/bench_cuda.json
python scripts/correctness.py  --device cuda \
                               --out results/correctness.json
CUDA_VISIBLE_DEVICES=1 python scripts/bench.py --device cuda --protocol both --mode both \
                               --out results/bench_cuda_rtxpro6000.json
python scripts/make_tables.py  --bench results/bench_cuda.json \
                               --correctness results/correctness.json \
                               --bench-hardware results/bench_cuda_rtxpro6000.json
\end{verbatim}

\noindent
\texttt{scripts/common.py} holds the measurement harness and the weight
synchronisation used by both entry points; \texttt{scripts/bench.py} sweeps the
configuration grid; \texttt{scripts/correctness.py} performs the equivalence
tests; \texttt{scripts/make\_tables.py} renders the tables. Kernel sizes are
never hard-coded in the table generator: it reads them from the recorded
arguments in the benchmark JSON, so the protocol described in a caption is
always the protocol that was measured.

\subsection{Measurement protocol}

\paragraph{Timing.} CUDA events bracket each individual iteration, so device
time is measured rather than host-side launch time, and the distribution over
iterations is retained rather than only its mean. Each configuration runs $20$
warmup iterations --- enough to cover JIT compilation and
cuDNN algorithm selection --- followed by $50$ timed iterations. The device is first driven to a steady clock
state; measured from idle, the smallest configurations are timed during clock
ramp-up and their means inflate by up to $2\times$, which is enough to distort a
geometric mean taken over the sweep. We report the
geometric mean of per-configuration ratios over the sweep; the raw JSON also
carries median, standard deviation and the $10$th/$90$th percentiles for every
configuration, which we recommend inspecting before quoting any single figure.

\paragraph{Memory.} \verb|torch.cuda.reset_peak_memory_stats| followed by one
untimed step and \verb|torch.cuda.max_memory_allocated|. This measures
allocator high-water mark, not reserved memory.

\paragraph{Weight synchronisation.} Correctness comparisons construct a
reference module and a fused module from the same seed and then copy every
learnable tensor from the former into the latter, so the two modules are the
same function. The base path maps
\verb|base_conv.weight|/\verb|.bias|/\verb|base_scale.weight| onto the fused
module's corresponding parameters, and level $\ell$ maps
\verb|wavelet_convs[l].weight| and \verb|wavelet_scale[l].weight|. The subband
axis is packed identically in both ($\text{channel} = 4c+s$ with
$s\in(\mathrm{LL},\mathrm{LH},\mathrm{HL},\mathrm{HH})$); rather than assume
this, \texttt{correctness.py} drives the reference's own filter bank with a
one-hot $2\times2$ patch and checks the resulting coefficient order and signs
against Eq.~\eqref{eq:butterfly}.

\paragraph{Cotangent.} Backward comparisons use a fixed random cotangent rather
than \verb|.sum()|, which would leave sign-cancelling errors undetected.

\paragraph{Tolerances.} \texttt{fp32} is held to $\text{atol}=10^{-5}$,
$\text{rtol}=10^{-4}$; \texttt{fp16} to $2\times10^{-2}$/$10^{-2}$; \texttt{bf16}
to $10^{-1}$/$5\times10^{-2}$. These follow from the accumulated rounding of a
$k^2$-term dot product in the respective format rather than being tuned to make
the test pass. We note that an absolute tolerance of $10^{-7}$, sometimes quoted
for comparisons of this kind, is not attainable for a multi-level accumulation
in \texttt{fp32} and should not be expected.

\subsection{Configuration grid}

$C\in\{32,64,128\}$; $H=W\in\{128,256,512\}$; batch size $8$;
$L\in\{1,\dots,5\}$; $k=5$ for WTConv and, under the homogeneous
protocol, for the plain-convolution baselines; $k=7$ for the
plain-convolution baselines under the drop-in protocol; \texttt{fp32} and
\texttt{fp16} for the latency and memory sweep, with \texttt{bf16} additionally
covered by the equivalence tests. Each protocol is measured both for a forward
pass and for a forward-and-backward step, giving the $504$
measurements reported in Section~\ref{sec:results}. The wavelet methods do not
depend on the protocol --- the two differ only in the kernel size handed to the
plain convolutions --- so they are measured once, under the homogeneous
protocol, and every reported ratio shares that denominator. Baselines: \emph{depthwise} is
\verb|nn.Conv2d(C, C, k, groups=C, padding='same')| and \emph{dense} is
\verb|nn.Conv2d(C, C, k, padding='same')|.

\subsection{Environment}

The environment record (GPU, compute capability, HBM capacity, PyTorch, CUDA,
and cuDNN versions) is captured automatically into every results JSON and
rendered into the manuscript by the table generator, so the reported hardware
cannot drift from the hardware actually used.

\subsection{Claim-to-evidence map}

\begin{table}[h]
\centering
\small
\begin{tabular}{p{0.44\linewidth}p{0.48\linewidth}}
\toprule
Claim & Evidence \\
\midrule
Haar analysis is self-inverse and self-adjoint & Proposition~\ref{prop:selfinverse}; verified exactly over the rationals by \texttt{correctness.py} \\
Single-pass synthesis & Proposition~\ref{prop:bitindexed} \\
Reference moves $7N+\tfrac{43}{3}N(1-4^{-L})$ elements & Per-primitive accounting, Section~\ref{sec:memorybound}, read off the reference implementation stage by stage \\
Operator is bandwidth-bound & $I\in[1.62,1.63]$ FLOP/byte in \texttt{fp32} ($[3.23,3.27]$ in \texttt{fp16}) against $I^\star\approx50$, Section~\ref{sec:memorybound} \\
Fusion is exact, not approximate & Each step is an algebraic identity; residuals reported by \texttt{correctness.py} in Table~\ref{tab:correctness} against the tolerances above \\
Fused form moves $7.00N$ ($L{=}1$) to $8.33N$ elements & Per-stage accounting, Section~\ref{sec:prediction}; not converted into a predicted latency ratio \\
Measured speedup & Tables~\ref{tab:latency} and~\ref{tab:latency-train}, against the reference and against plain convolutions at matched kernel size and at the drop-in kernel size \\
Measured peak memory & Table~\ref{tab:memory} (training step) and Table~\ref{tab:memory-inference} (inference) \\
WTConv's receptive field exceeds the baseline's & $5\cdot2^{L}$ against $7$, Table~\ref{tab:rf}; stated so the drop-in comparison is not read as a receptive-field match \\
Results reproduce on a second, architecturally different GPU & Table~\ref{tab:hardware}, \Cref{sec:results-hardware} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Known gaps}

For transparency we list what the artifact does \emph{not} establish. There is
no accuracy result connecting the fused kernels to a downstream task, and this
paper reports no end-to-end training measurement: every reported ratio is
layer-level. There is no \texttt{torch.compile} arm on either side. Timing uses a
single resident input tensor, so no host-to-device transfer or data loading is
included. The layer-level sweep is repeated on a second GPU
(\Cref{sec:results-hardware}), but the end-to-end network measurements of
\Cref{sec:results-endtoend} and the traffic profiling of \Cref{app:traffic}
are each still represented by one device only. The fused kernels are
compiled with \verb|--use_fast_math|; the reassociation identities of
Section~\ref{sec:method} are exact, but that flag additionally relaxes IEEE
semantics within a kernel, so the residuals of Table~\ref{tab:correctness} are
what we claim, rather than bit-identity with the reference.
