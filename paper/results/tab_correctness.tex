% PLACEHOLDER -- regenerate with:
%   python scripts/correctness.py --device cuda --out ../results/correctness.json
%   python scripts/make_tables.py --correctness ../results/correctness.json
% The structural rows are proved in Appendix A and verified symbolically by
% scripts/correctness.py; the numerical rows require a CUDA device.
\begin{table}[t]
\centering
\caption{Numerical agreement with the reference implementation, $C{=}32$,
$H{=}W{=}64$, $B{=}2$, $k{=}3$. Entries are the maximum absolute deviation over
the whole tensor. Because every step of Section~\ref{sec:method} is an algebraic
identity, the residual is floating-point reassociation error alone, and it stays
at the level set by a $k^2$-term accumulation in the working precision.
\textbf{Numbers below are to be regenerated on the target device by
\texttt{scripts/correctness.py}}; the two structural rows are exact and machine
independent.}
\label{tab:correctness}
\begin{tabular}{llccccc}
\toprule
Backend & dtype & $L$ & forward & $\nabla_{\!X}$ & $\nabla_{\!W}$ & $\nabla_{\!s}$ \\
\midrule
\multicolumn{7}{l}{\itshape Structural properties (exact, verified symbolically)} \\
\quad $\mathbf{H}=\mathbf{H}^{\!\top}=\mathbf{H}^{-1}$ & --- & --- & \multicolumn{4}{c}{holds exactly ($\lVert\mathbf{H}^2-\mathbf{I}\rVert_\infty=0$)} \\
\quad subband packing $(\mathrm{LL},\mathrm{LH},\mathrm{HL},\mathrm{HH})$ & --- & --- & \multicolumn{4}{c}{matches reference exactly} \\
\midrule
\multicolumn{7}{l}{\itshape Measured deviation} \\
Fused (CUDA) & \texttt{fp32} & 1 & --- & --- & --- & --- \\
Fused (CUDA) & \texttt{fp32} & 3 & --- & --- & --- & --- \\
Fused (CUDA) & \texttt{fp32} & 5 & --- & --- & --- & --- \\
\addlinespace[2pt]
Fused (CUDA) & \texttt{fp16} & 1 & --- & --- & --- & --- \\
Fused (CUDA) & \texttt{fp16} & 3 & --- & --- & --- & --- \\
Fused (CUDA) & \texttt{fp16} & 5 & --- & --- & --- & --- \\
\addlinespace[2pt]
Fused (CUDA) & \texttt{bf16} & 3 & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}
